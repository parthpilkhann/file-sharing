<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <!-- in form: action is specified "/upload" which will take us to the "upload url" and request method (post) is given -->
    <!-- also if there is no action present then the same page will reload -->
    <form method="post"
        style="display: grid; gap: 1rem; max-width: 500px; margin: 50px auto; grid-template-columns: auto 1fr;">
        <label for="password">Password.: </label>
        <input type="password" id="password" name="password" required />
        <button style="grid-column: span 2;" type="submit">Download</button>
        <% if (locals.error){ %>
            <div style="margin-bottom: 1rem; color: red;"> 
                Incorrect Password
            </div>
            <% locals.error = false %> 
        <% } %>
    </form>
</body>

</html>